---
const navLinks = [
  { href: '/', label: 'Home' },
  { href: '/courses', label: 'Video Library' },
  { href: '/about', label: 'Instructors' },
  { href: '/bookings', label: 'Book a Lesson' },
  { href: '/pricing', label: 'Pricing' },
  { href: '/free-lesson', label: 'Free Lesson' },
];
---

<header id="site-header" class="fixed top-0 left-0 right-0 z-50">
  <!-- Announcement Bar -->
  <div class="bg-amber-500 h-10 flex items-center justify-center px-4 overflow-hidden">
    <p class="text-slate-950 text-xs sm:text-sm font-medium text-center">
      <span class="font-bold">New:</span> 2026 Jack Links Cup Champion: Gebben Miles — Karen interviews him the day after the win
      <a href="/courses" class="underline underline-offset-2 font-semibold hover:text-slate-800 transition-colors ml-1">
        Watch now →
      </a>
    </p>
  </div>

  <!-- Nav -->
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-16 flex items-center justify-between bg-slate-950/90 backdrop-blur-lg border-b border-slate-800/50">
    <!-- Logo -->
    <a href="/" class="flex items-center group">
      <img src="/images/sca-logo.svg" alt="Sporting Clays Academy" class="h-8 w-auto group-hover:opacity-90 transition-opacity" />
    </a>

    <!-- Desktop Nav -->
    <div class="hidden md:flex items-center gap-7">
      {navLinks.map(link => (
        <a
          href={link.href}
          class="text-sm text-slate-300 hover:text-white transition-colors"
        >
          {link.label}
        </a>
      ))}
      <!-- Auth: Sign In button (shown when logged out) -->
      <button
        id="header-signin-btn"
        onclick="(window as any).openAuthModal && (window as any).openAuthModal()"
        class="text-sm text-slate-300 hover:text-white transition-colors"
      >
        Sign In
      </button>
      <!-- Auth: User menu (shown when logged in) -->
      <div id="header-user-menu" class="hidden relative group">
        <button class="flex items-center gap-2 text-sm text-slate-300 hover:text-white transition-colors">
          <svg class="w-4 h-4 text-amber-500" fill="currentColor" viewBox="0 0 20 20">
            <path fill-rule="evenodd" d="M10 9a3 3 0 100-6 3 3 0 000 6zm-7 9a7 7 0 1114 0H3z" clip-rule="evenodd"/>
          </svg>
          <span id="header-user-email" class="max-w-[140px] truncate">Member</span>
          <svg class="w-3 h-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
          </svg>
        </button>
        <!-- Dropdown -->
        <div class="absolute right-0 top-full mt-2 w-48 bg-slate-900 border border-slate-700 rounded-xl shadow-xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 z-50">
          <a href="/account" class="block px-4 py-3 text-sm text-slate-300 hover:text-white hover:bg-slate-800 rounded-t-xl transition-colors">
            My Account
          </a>
          <button
            id="header-signout-btn"
            class="w-full text-left px-4 py-3 text-sm text-slate-300 hover:text-white hover:bg-slate-800 rounded-b-xl transition-colors border-t border-slate-700/50"
          >
            Sign Out
          </button>
        </div>
      </div>
      <a
        href="/pricing"
        class="bg-gradient-to-br from-amber-400 to-amber-500 hover:from-amber-300 hover:to-amber-400 text-slate-950 font-bold text-sm px-5 py-2.5 rounded-lg shadow-md shadow-amber-500/40 hover:shadow-lg hover:shadow-amber-500/60 hover:-translate-y-0.5 transition-all duration-200"
      >
        Start Learning
      </a>
    </div>

    <!-- Mobile Menu Toggle -->
    <button
      id="mobile-menu-btn"
      class="md:hidden flex flex-col gap-1.5 cursor-pointer p-2 z-[70] relative"
      aria-label="Toggle menu"
      aria-expanded="false"
    >
      <span class="block w-6 h-0.5 bg-slate-300 transition-all duration-300 origin-center" id="ham-top"></span>
      <span class="block w-6 h-0.5 bg-slate-300 transition-all duration-300" id="ham-mid"></span>
      <span class="block w-6 h-0.5 bg-slate-300 transition-all duration-300 origin-center" id="ham-bot"></span>
    </button>

    <!-- Mobile Menu — hidden by default via Tailwind, not inline style -->
    <!-- SCA has announcement bar (40px) + nav (64px) = 104px total header height -->
    <div
      id="mobile-menu"
      class="md:hidden hidden fixed inset-x-0 bottom-0 bg-slate-950 z-[60] flex-col items-center justify-start pt-10 gap-6 border-t border-slate-800"
      style="top: 104px;"
    >
      {navLinks.map(link => (
        <a
          href={link.href}
          class="text-lg text-white hover:text-amber-400 transition-colors"
        >
          {link.label}
        </a>
      ))}
      <a
        href="/pricing"
        class="bg-gradient-to-br from-amber-400 to-amber-500 hover:from-amber-300 hover:to-amber-400 text-slate-950 font-bold px-8 py-3 rounded-lg shadow-lg shadow-amber-500/40 hover:shadow-xl hover:shadow-amber-500/60 transition-all duration-200 mt-4"
      >
        Start Learning
      </a>

      <!-- Mobile Auth -->
      <button
        id="mobile-signin-btn"
        class="text-lg text-slate-300 hover:text-amber-400 transition-colors"
        onclick="(window as any).openAuthModal && (window as any).openAuthModal(); document.getElementById('mobile-menu')?.classList.add('hidden'); document.getElementById('mobile-menu')?.classList.remove('flex');"
      >
        Sign In
      </button>
      <button
        id="mobile-signout-btn"
        class="hidden text-lg text-slate-300 hover:text-amber-400 transition-colors"
      >
        Sign Out
      </button>

      <div class="absolute bottom-8 text-slate-500 text-xs text-center px-8">
        30-Day Money-Back Guarantee • Cancel Anytime
      </div>
    </div>
  </nav>

  <script>
    const btn = document.getElementById('mobile-menu-btn');
    const menu = document.getElementById('mobile-menu');
    const hamTop = document.getElementById('ham-top');
    const hamMid = document.getElementById('ham-mid');
    const hamBot = document.getElementById('ham-bot');

    btn?.addEventListener('click', () => {
      const isOpen = !menu?.classList.contains('hidden');

      if (isOpen) {
        menu?.classList.add('hidden');
        menu?.classList.remove('flex');
        btn?.setAttribute('aria-expanded', 'false');
        hamTop?.classList.remove('rotate-45', 'translate-y-2');
        hamMid?.classList.remove('opacity-0');
        hamBot?.classList.remove('-rotate-45', '-translate-y-2');
      } else {
        menu?.classList.remove('hidden');
        menu?.classList.add('flex');
        btn?.setAttribute('aria-expanded', 'true');
        hamTop?.classList.add('rotate-45', 'translate-y-2');
        hamMid?.classList.add('opacity-0');
        hamBot?.classList.add('-rotate-45', '-translate-y-2');
      }
    });

    // Close menu on nav link click
    menu?.querySelectorAll('a').forEach(link => {
      link.addEventListener('click', () => {
        menu?.classList.add('hidden');
        menu?.classList.remove('flex');
        btn?.setAttribute('aria-expanded', 'false');
        hamTop?.classList.remove('rotate-45', 'translate-y-2');
        hamMid?.classList.remove('opacity-0');
        hamBot?.classList.remove('-rotate-45', '-translate-y-2');
      });
    });
  </script>
</header>

<!-- Spacer: announcement bar (40px) + nav (64px) = 104px -->
<div class="h-[104px]"></div>
